{{ if contains .Input "\n" -}}
locals {
  input = <<EOT
{{ .Input }}EOT
}

{{ end -}}
output "hash_sha256" {
{{- if contains .Input "\n" }}
  value = provider::corefunc::hash_sha256(local.input)
{{- else }}
  value = provider::corefunc::hash_sha256("{{ .Input }}")
{{- end }}
}

#=> {{ .Expected }}
